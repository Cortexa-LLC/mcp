# Test coverage
cover.out

# Compiled binaries
build/

# Server binaries built in place rather than into build/ — a bare `go build`
# in a server directory names its output after the directory.
/src/kg/kg
/src/markitdown/markitdown-mcp
